import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ApplicationsListComponent } from './applications-list.component';
import { ApiService } from '../index';

describe('ApplicationsListComponent', () => {
  // let component: ApplicationsListComponent;
  // let fixture: ComponentFixture<ApplicationsListComponent>;
  // let apiService: any;

  // class ApiServiceMock {
  //   getApplications() = () => Observable.of
  // }
  //
  // beforeEach(async(() => {
  //
  //
  //
  //   TestBed.configureTestingModule({
  //     providers: [ ApiService ],
  //     declarations: [ ApplicationsListComponent ]
  //   });
  //
  //   TestBed.overrideComponent(ApplicationsListComponent, {
  //     set: {
  //       providers: [
  //         { provide: ApiService, useClass: FundingPlanServiceMock },
  //       ]
  //     }
  //   }))
  //
  //   apiService = TestBed.get('ApiService');
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ApplicationsListComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
  //
  // it('should get applications', () => {
  //   const spy = spyOn(apiService, 'getApplications');
  //
  //   component.ngOnInit();
  //   expect(spy.wasCalled).toEqual(true);
  // });

});
